/*
 * 1. Grayscale only.
 * 2. Use this function prototype.
 * 3. Each time the function is called, a new variant should be generated.
 */
void circle(float X, float Y, float R) {
  float d = 2 * R;
  ellipse(X, Y, d, d);
}
